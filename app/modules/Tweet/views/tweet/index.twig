{% extends 'admin/index.twig' %}
{% block content %}

<div class="row page-header-box">
    <ul class="breadcrumb">
        <li><a href="../../../admin">Dashboard</a></li>
        <li><a href="../../tweetset">Tweetset</a></li>
        <li class="active">{{ title }}</li>
    </ul>
    <div class="col-xs-10">
        <h3>{{ title }}</h3>
    </div>
    <div class="col-xs-2">
        <div class = "row">
            <a href="#" id="btn-tweet-add" class="btn btn-success btn-sm pull-right"><i class="fa fa-plus"></i> Add tweet</a>
            <a href="../random-tweet/{{tweetset_id}}" class="btn btn-danger  btn-sm pull-right" ><i class="fa fa-fire"></i> Randomize </a>
        </div>
    </div>
</div>
<div class="row">
    <div class="col-lg-12">
        <div class="table-responsive">
            <table class="table table-striped table-condensed">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>Name</th>
                        <th>Text</th>
                        <th>Mentions</th>
                        <th>Hashtags</th>
                        <th>Media</th>
                        <th>Updated At</th>
                        <th>Created At</th>
                        <th style="width:20%" class="text-center">Action</th>
                    </tr>
                </thead>
                <tbody id="tweet-table">
                    {% for tweet in tweets %}
                    <tr id="tweet-row-{{ tweet.id }}">
                        <td>{{ tweet.id }}</td>
                        <td>{{ tweet.name }}</td>
                        <td>{{ tweet.text }}</td>
                        <td>{{ tweet.mentions }}</td>
                        <td>{{ tweet.hashtags }}</td>
                        <td>{{ tweet.media }}</td>
                        <td>{{ tweet.updated_at }}</td>
                        <td>{{ tweet.created_at }}</td>
                        <td class="text-center">
                            <a data-id="{{ tweet.id }}" class="btn btn-xs btn-primary btn-tweet-edit" href="#"><i class="fa fa-edit fa-fw"></i>Edit</a>
                            <a data-id="{{ tweet.id }}" class="btn btn-xs btn-danger btn-tweet-delete" href="#"><i class="fa fa-times fa-fw"></i>Remove</a>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
<div class="modal fade" id="tweet-modal">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                <h4 class="modal-title" id="modal-title">Tweet Form</h4>
            </div>
            <div class="modal-body">
                {% include '@tweet/tweet/form.twig' %}
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default btn-sm" data-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-success btn-sm" id="btn-tweet-save" data-method="">Save</button>
            </div>
        </div>
    </div>
</div>
{% endblock %}